{
  "compilerOptions": {
    "allowJs": true, // Allows JavaScript files to be compiled.
    "allowSyntheticDefaultImports": true, // Allows default imports from modules with no default export.
    "esModuleInterop": true, // Enables emit interoperability between CommonJS and ES Modules.
    "isolatedModules": true, // Ensures each file can be transpiled separately (necessary for Vite).
    "noImplicitAny": false,
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "strict": false, // Enables all strict type-checking options.
    "skipLibCheck": true, // Skips type checking of declaration files.
    "jsx": "react-jsx", // For React 17+ to use the new JSX transform.
    "lib": ["es2015", "dom"], // Changed to es2015 for better compatibility
    "moduleResolution": "node", // Resolves modules using Node's resolution algorithm.
    "types": ["chrome", "node", "react", "react-dom"],
    "baseUrl": ".", // Base directory for non-relative module names.
    "paths": {
      "@/*": ["./*"]
    },
    "noEmit": true, // Prevents TypeScript from emitting files.
    "resolveJsonModule": true, // Allows importing JSON files.
    "target": "es2015", // Changed to es2015 for better compatibility
    "useDefineForClassFields": false, // Disable this to avoid WeakMap issues with private fields
    "downlevelIteration": true, // Enable downlevel iteration for better ES5 compatibility
    "importHelpers": false // Disable importing helpers to avoid dependency issues
  },
  "exclude": [
    "node_modules",
    "dist0", // Excludes the output directory for builds.
    "babel.config.js", // Excludes Babel configuration file.
    "metro.config.js", // Excludes Metro configuration file.
    "jest.config.js" // Excludes Jest configuration file if it's present.
  ]
}
